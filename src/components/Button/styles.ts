import { StyleSheet } from 'react-native';
import general from '../../styles/general';

const styles = StyleSheet.create({
  button: {
    ...general.defaultButton,
    width: '100%',
  },
  buttonText: {
    ...general.defaultButtonText,
  },
});

export default styles;
